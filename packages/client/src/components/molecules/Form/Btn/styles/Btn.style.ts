import {Button} from "@mui/material";
import {styled} from "@mui/material/styles";

export const CustomBtn = styled(Button)(({theme, variant}) => ({
    // bgColor: theme.palette.primary.main,
    borderRadius: `4px`,
    padding: '0.5rem 1rem',
    width: '100%',
    margin: '0.5rem 0',


}))