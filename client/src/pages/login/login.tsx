import React from 'react';

interface LOGIN_PROPS {

}

const Login: React.FC<LOGIN_PROPS> = (props): JSX.Element => {
    return (
        <>
            <h1> Its Login Component</h1>
            
        </>
    );
};

export default Login;